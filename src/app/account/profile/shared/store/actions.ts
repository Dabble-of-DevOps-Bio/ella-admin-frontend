import { createAction } from '@ngrx/store';

export class AccountProfilePageActions {
  /* tslint:disable:typedef */
  public static resetState = createAction(
    '[Account Profile Page] Reset State'
  );
  /* tslint:enable:typedef */
}
